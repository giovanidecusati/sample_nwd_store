export interface IProductModel {
  productId: number;
  productName: string;
  productDescription: string;
  productUri: string;
  productPrice: number;
  productImageUrl: string;
  productRating: number;
  categoryName: string;
  categoryUri: string;
}
